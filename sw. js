self.addEventListener('install', (event) => {
  self.skipWaiting();
});

self.addEventListener('fetch', (event) => {
  // Hier könnte man Caching einbauen, 
  // aber für die Installation reicht die Anwesenheit dieses Events.
  event.respondWith(fetch(event.request));
});
